# --- server ---
PORT=4000
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3000

# --- chain ---
# 143 = Monad mainnet, 10143 = Monad testnet
CHAIN_ID=10143
# Optional override; defaults to the public RPC for CHAIN_ID
RPC_URL=

# --- DeltaMon contracts (leave empty until deployed; the API then serves demo data) ---
VAULT_ADDRESS=

# --- keeper ---
KEEPER_ENABLED=false
# Without a key the keeper runs in dry-run mode and only logs what it would do.
KEEPER_PRIVATE_KEY=
KEEPER_INTERVAL_MS=60000
REBALANCE_THRESHOLD_BPS=200

# --- Perpl (hedge venue) ---
PERPL_API_URL=https://testnet.perpl.xyz/api
PERPL_API_KEY=
PERPL_API_KEY_SECRET=
